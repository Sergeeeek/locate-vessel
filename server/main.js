import { Meteor } from 'meteor/meteor';
import '../imports/api/vessels/api';
import '../imports/api/vessels/fixtures';
import '../imports/api/vesselLocation/api';

Meteor.startup(() => {
  // code to run on server at startup
});
